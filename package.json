{
  "name": "dink-url-manager",
  "version": "1.0.1",
  "description": "Discord webhook filtering proxy for DinkPlugin with allowlist/denylist support",
  "type": "module",
  "main": "src/worker.tsx",
  "scripts": {
    "dev": "wrangler dev",
    "build": "vite build",
    "deploy": "wrangler deploy && wrangler d1 migrations apply DB --remote",
    "migrate": "wrangler d1 migrations apply DB --remote",
    "format": "prettier --write \"src/**/*.{ts,tsx}\"",
    "format:check": "prettier --check \"src/**/*.{ts,tsx}\""
  },
  "dependencies": {
    "hono": "^4.12.16"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.35.0",
    "@hono/vite-dev-server": "^0.25.0",
    "prettier": "3.9.6",
    "typescript": "^5.9.3",
    "vite": "^6.4.2",
    "wrangler": "^4.87.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "cloudflare": {
    "bindings": {
      "DISABLE_NEW_CONFIGS": {
        "description": "Set to `true` to block creation of new webhook configs, e.g. to freeze a deployment after setup."
      }
    }
  }
}
